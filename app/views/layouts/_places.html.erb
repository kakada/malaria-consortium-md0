<div id="places_title">
  <% if @navbar_places.any? %>
    <%= @navbar_places.first.class.to_s.underscore.humanize.pluralize %>
  <% elsif @place %>
    <%= @place.name %>
  <% else %>
    Provinces
  <% end %>
</div>
<div id="places">
  <% @navbar_places.each do |place| %>
    <%= link_to "#{place.code} #{place.name}", current_url_for(:place => place.id) %><br/>
  <% end %>
</div>
